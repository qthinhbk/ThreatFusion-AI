# Snort Rules

`local_ics.rules` contains lab-oriented ICS network signatures.

The sample `snort.lua` is intentionally minimal. Real deployments should use the organization's production Snort configuration and include `local_ics.rules` from that config.
